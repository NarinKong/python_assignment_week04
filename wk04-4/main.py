import string_operations as so
sample_string = "hello World"

print("Original:", sample_string)
print("Reversed:", so.rev(sample_string))
print("Capitalized:", so.capi(sample_string))
print("Lowercase:", so.lower(sample_string))
print("Uppercase:", so.upper(sample_string))